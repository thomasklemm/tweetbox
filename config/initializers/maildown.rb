# Maildown::MarkdownEngine.set do |text|
#   renderer = Redcarpet::Render::HTML.new(hard_wrap: true)
#   carpet = Redcarpet::Markdown.new(renderer, {})
#   carpet.render(text).html_safe

#   TweetPipeline.new(text).to_html
# end
