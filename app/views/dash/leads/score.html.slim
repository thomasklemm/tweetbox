/ score leads

= logo_header score_title(params[:score])

/ search form
= render 'search_form'

/ page entries
= page_entries_info @leads, entry_name: score_title(params[:score]).try(:downcase)

#leads.leads
  = render partial: 'lead', collection: @leads

= paginate @leads, window: 2
