<% if search %>
  created a search query for "<%= search.query %>" on the <%= search.project.name %> project (<%= pluralize search.project.searches.size, 'search' %>)
<% else %>
  created a search query which has since been removed
<% end %>
