/ A custom layout for all views rendered by the project controller and its subcontrollers
- content_for :content do
  .row
    .span2
      ul.project-nav class='nav nav-pills nav-stacked'
        li
          b Tweets
          ul.tweets-subnav class='nav nav-pills nav-stacked'
            = active_link_to 'Incoming', incoming_project_tweets_path(@project)
            = active_link_to 'Open', open_project_tweets_path(@project)
            = active_link_to 'Resolved', resolved_project_tweets_path(@project)
            = active_link_to 'New status', new_project_status_path(@project)

        b Project
        = active_link_to 'Twitter Accounts', project_twitter_accounts_path(@project)
        = active_link_to 'Searches', project_searches_path(@project)

    .span10
      / any flash messages
      = render 'flashes'

      = content_for?(:project_content) ? yield(:project_content) : yield

= render template: 'layouts/application'


