/ conversation for first tweet in conversation partial
- tweet &&= tweet.decorate
- first_tweet &&= first_tweet.decorate

/ .conversation_for_tweet_1(will use ids)
= div_for tweet, :conversation_for do

    / first tweet in conversation
    = render first_tweet
