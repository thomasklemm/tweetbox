/ .tweet-wrapper id="#{ dom_id(tweet) }_wrapper"
/   - if tweet.incoming?
/     = render partial: 'tweets/tweet', collection: tweet.previous_tweets, locals: { main_tweet: nil } if tweet.previous_tweets.size > 0
/     = render partial: 'tweets/tweet', collection: [tweet], locals: { main_tweet: tweet }
/     = render partial: 'tweets/tweet', collection: tweet.future_tweets, locals: { main_tweet: nil } if tweet.future_tweets.size > 0

/   - if tweet.resolved?
/     p Resolved tweet

/   - if tweet.posted?
/     p Posted tweet

/   - if tweet.conversation?
/     p Conversation tweet
