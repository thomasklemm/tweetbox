/ tweet partial
- tweet &&= tweet.decorate

/ incoming tweet
- if tweet.incoming?
  = render 'tweets/incoming_tweet', tweet: tweet

/ resolved tweet
- if tweet.resolved?
  = render 'tweets/resolved_tweet', tweet: tweet

/ posted tweet
- if tweet.posted?
  = render 'tweets/posted_tweet', tweet: tweet

/ conversation tweet
- if tweet.conversation?
  = render 'tweets/conversation_tweet', tweet: tweet
