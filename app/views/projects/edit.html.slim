h1 Projects#edit
p Find me in app/views/projects/edit.html.slim

= render 'form'

= link_to 'Destroy project', account_project_path(@project.account, @project), method: :delete, confirm: 'Are you sure?'
